meta {
  name: Search Models
  type: http
  seq: 3
}

post {
  url: {{model_ms_url}}/api/v1/models/search
  body: json
  auth: inherit
}

body:json {
  {
    "source": "RTL433"
  }
}

settings {
  encodeUrl: true
}
